package ${packageName};

import android.content.Context;

import javax.inject.Inject;


public class ${className}Router implements ${className}Contract.Router {

    @Inject
    public ${className}Router() {}

    // add routing functions
}
